/**Ejemplos básicos */

/**Ejemplo 1
 * 
    const numeros = [1, 2, 3, 4, 5]

    const numToString = numeros.map(num => num.toString())
    console.log(numToString) 
 */


/**Ejemplo 2
    const numeros = [2, 4, 6, 8, 10]

    const multiplicado = numeros.map((numero, indice) => numero * indice)
    console.log(multiplicado)
 */

/**Ejemplo 3 */
